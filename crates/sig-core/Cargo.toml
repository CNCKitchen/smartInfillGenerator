[package]
name = "sig-core"
version = "0.1.0"
edition = "2021"
license = "LicenseRef-Proprietary"
publish = false

[features]
default = ["parallel"]
parallel = ["dep:rayon"]

[dependencies]
rayon = { version = "1.10", optional = true }

[[bin]]
name = "bench"
path = "src/bin/bench.rs"
