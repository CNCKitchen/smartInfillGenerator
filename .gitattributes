# 3MF golden fixtures are byte-compared against the writer's LF-only output;
# keep them LF on every platform so the pin is portable (no autocrlf munging).
crates/sig-core/tests/golden/*.txt text eol=lf
